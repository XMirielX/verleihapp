const http = require("http");

const server = http.createServer((req, res) => {
  res.write("Server funktioniert!");
  res.end();
});

server.listen(3000, () => {
  console.log("Server läuft auf Port 3000");
});